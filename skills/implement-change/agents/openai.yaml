interface:
  display_name: "Implement Change"
  short_description: "Build one story or bugfix"
  default_prompt: "Use $implement-change to read one validated epic-scoped or standalone story or bugfix and implement its scoped code, tests, docs, config, or migration changes."
